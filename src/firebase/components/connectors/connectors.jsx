import React from 'react';
import Account from '../Account/account';
import SignIn from '../Authentication/SignIn';
import SignUp from '../Authentication/signUp';

const Connectors = ({uid}) => {
    return (
        <div>
            <Account/>
          
        </div>
    );
}

export default Connectors;
